class LearningConfigurations:
    """
    This is a collection of static configurations, required by the learning model
    """
    # Dictates how many times the sequence would run in every model operation (training, test, run)
    NUMBER_OF_LEARNING_CYCLES = 10
